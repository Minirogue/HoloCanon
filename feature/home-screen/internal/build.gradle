plugins {
    id "holocanon.android.library"
    id "holocanon.hilt"
}

android {
    namespace 'com.minirogue.holocanon.feature.home.screen.internal'
    resourcePrefix 'home_screen'

    buildFeatures {
        compose true
    }

    composeOptions {
        kotlinCompilerExtensionVersion = "$composeCompilerVersion"
    }
}

dependencies {
    api projects.feature.homeScreen.public

    implementation projects.library.composeExt.public
}
