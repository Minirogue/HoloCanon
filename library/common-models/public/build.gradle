plugins{
    id 'holocanon.multiplatform.library'
}

holocanon {
    hilt()
    serialization()
}

kotlin {
    jvm() // module is consumed by :api

    sourceSets {
        commonMain {
            dependencies {
            }
        }
    }
}
