plugins{
    id 'holocanon.multiplatform.library'
}

holocanon {
    serialization()
}

kotlin {
    jvm() // module is consumed by :api

    sourceSets {
        commonMain {
            dependencies {
            }
        }
    }
}
