plugins{
    id 'holocanon.multiplatform.library'
    id 'holocanon.serialization'
}

holocanon {
    hilt()
}

kotlin {
    jvm() // module is consumed by :api

    sourceSets {
        commonMain {
            dependencies {
            }
        }
    }
}
