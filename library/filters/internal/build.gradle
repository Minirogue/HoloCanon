plugins {
    id "minirogue.multiplatform.library"
}

minirogue {
    platforms {
        android()
        ios()
    }
    metro()
}

kotlin.sourceSets.commonMain.dependencies {
    api projects.library.filters.public

    implementation projects.core
    implementation projects.library.commonModels.public
    implementation projects.library.settings.public
}

kotlin.sourceSets.commonTest.dependencies {
    implementation projects.core.testing
    implementation projects.library.commonTest.testing
    implementation projects.library.coroutineExt.testing
    implementation projects.library.filters.testing
    implementation projects.library.settings.testing
}
