plugins {
    id "holocanon.android.library"
}

android {
    buildFeatures {
        viewBinding true
    }
}

dependencies {
    // Android dependencies
    api libs.android.material

    // project dependencies
    api project(":library:filters:public")
}