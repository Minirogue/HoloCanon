plugins {
    id("minirogue.multiplatform.library")
}

minirogue {
    platforms {
        jvm()
        ios()
    }
}

kotlin.sourceSets.commonMain.dependencies {
    api(libs.kotlinx.coroutines.test)
    api(projects.library.commonTest.testing)
    api(projects.library.coroutineExt.public)
}