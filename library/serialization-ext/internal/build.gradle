plugins {
    id 'holocanon.multiplatform.library'
}

holocanon {
    hilt()
    serialization()
}

kotlin {
    jvm() // used by :api

    sourceSets {
        commonMain {
            dependencies {
            }
        }
    }
}
